let plants = []

export const addPlant = () => {
  const plantObject = [];
  for (let i = 0; i < plants.length; i++) {
    
    addPlant.push(plantObject)
  }
  return addPlant
}

export const usePlants = () => {
  return p
}

